---
phase: 03-progress-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/db/schema.ts]
autonomous: true
requirements: [PROGRESS-03]

must_haves:
  truths:
    - "D1 database has activity_completion and course_progress tables"
    - "Drizzle schema accurately reflects the new tables"
  artifacts:
    - path: "app/db/schema.ts"
      provides: "New tables for progress tracking"
    - path: "migrations/0002_add_progress_tables.sql"
      provides: "D1 migration script"
  key_links:
    - from: "app/db/schema.ts"
      to: "D1 Database"
      via: "Drizzle Migration"
---

<objective>
Update the database schema to support activity completion tracking and course-level progress persistence.

Purpose: Provide the necessary storage layer for tracking user progress through courses.
Output: Updated Drizzle schema and applied D1 migrations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/db/schema.ts

<interfaces>
From app/db/schema.ts:
```typescript
export const user = sqliteTable("user", { ... });
export const courses = sqliteTable("courses", { ... });
export const activities = sqliteTable("activities", { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app/db/schema.ts with progress tables</name>
  <files>app/db/schema.ts</files>
  <action>
    Add the following tables to `app/db/schema.ts`:
    1. `activityCompletion`: 
       - `id`: text primary key
       - `userId`: references `user.id`
       - `activityId`: references `activities.id`
       - `completedAt`: timestamp
    2. `courseProgress`:
       - `id`: text primary key
       - `userId`: references `user.id`
       - `courseId`: references `courses.id`
       - `progressPercent`: integer (0-100)
       - `completedAt`: timestamp (optional)
       - `updatedAt`: timestamp
    
    Ensure relations are updated for `user`, `courses`, and `activities`.
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>Schema updated and validated by drizzle-kit.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply D1 migration</name>
  <files>migrations/*.sql</files>
  <action>
    1. Generate the migration: `npx drizzle-kit generate`
    2. Apply the migration to local D1: `npx wrangler d1 migrations apply leanlearn-db --local` (check wrangler.jsonc for database name)
  </action>
  <verify>
    <automated>npx wrangler d1 execute leanlearn-db --local --command "SELECT name FROM sqlite_master WHERE type='table' AND name IN ('activity_completion', 'course_progress');"</automated>
  </verify>
  <done>Tables exist in the local D1 database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema | Migration script must correctly reflect schema definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | D1 Migration | mitigate | Use drizzle-kit generate to ensure consistent SQL generation |
</threat_model>

<verification>
Verify tables in SQLite: `npx wrangler d1 execute leanlearn-db --local --command ".schema activity_completion"`
</verification>

<success_criteria>
- `activity_completion` and `course_progress` tables exist in D1.
- `app/db/schema.ts` includes the new table definitions and relations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-progress-tracking/03-01-SUMMARY.md`
</output>
