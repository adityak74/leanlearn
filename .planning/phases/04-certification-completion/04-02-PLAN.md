---
phase: 04-certification-completion
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [app/routes/certificate.$id.tsx, app/routes.ts]
autonomous: true
requirements: [REQ-CERT-02]

must_haves:
  truths:
    - "A dedicated route /certificate/:id renders the certificate"
    - "The certificate includes User Name, Course Title, and Issue Date"
    - "The page is optimized for printing (no sidebars, centered layout)"
  artifacts:
    - path: "app/routes/certificate.$id.tsx"
      provides: "Printable certificate view"
  key_links:
    - from: "app/routes/certificate.$id.tsx"
      to: "certificates/users/courses tables"
      via: "drizzle join query"
---

<objective>
Create a professional, print-optimized certificate view that learners can use to print or save their achievement.

Purpose: Provide a tangible reward for course completion.
Output: A new printable route /certificate/:id.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-certification-completion/04-01-SUMMARY.md
@app/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement certificate route and loader</name>
  <files>app/routes/certificate.$id.tsx, app/routes.ts</files>
  <action>
    - Register `route("certificate/:id", "routes/certificate.$id.tsx")` in `app/routes.ts`.
    - Create `app/routes/certificate.$id.tsx`.
    - Implement a `loader` that:
      - Authenticates the user.
      - Fetches the certificate by ID, joining with `user` and `courses` to get names.
      - Checks that the `userId` on the certificate matches the logged-in user.
      - Returns the certificate, user, and course data.
    - Implement a base React component that displays the certificate content (e.g., "Certificate of Completion", User Name, Course Name, Date).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Certificate route exists and securely loads data.</done>
</task>

<task type="auto">
  <name>Task 2: Add print-optimized CSS</name>
  <files>app/routes/certificate.$id.tsx</files>
  <action>
    - Add a `<style>` block or use inline styles with `@media print` in `app/routes/certificate.$id.tsx`.
    - Requirements:
      - `@page { size: A4 landscape; margin: 0; }`
      - Hide any UI elements (like a "Print" button) using a `.no-print` class.
      - Center the certificate content on the page.
      - Use `print-color-adjust: exact;` to ensure backgrounds/borders appear.
      - Add a "Print Certificate" button visible only on screen to trigger `window.print()`.
  </action>
  <verify>
    <automated>grep -q "@media print" app/routes/certificate.$id.tsx</automated>
  </verify>
  <done>Certificate page is styled for professional printing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public → Certificate | Certificate IDs (UUIDs) are public but the view requires auth and ownership check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | Unauthorized View | mitigate | Loader MUST verify that `session.userId === certificate.userId` before rendering. |
</threat_model>

<verification>
- Verify that the loader contains an ownership check.
- Verify that `@media print` styles are present in the file.
</verification>

<success_criteria>
- /certificate/:id route is functional.
- Only the certificate owner can view it.
- Page contains print-specific CSS.
</success_criteria>

<output>
After completion, create `.planning/phases/04-certification-completion/04-02-SUMMARY.md`
</output>
