---
phase: 04-certification-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/db/schema.ts, app/routes/course.$slug.tsx]
autonomous: true
requirements: [REQ-CERT-01]

must_haves:
  truths:
    - "Database contains a certificates table linked to users and courses"
    - "A certificate is automatically created in D1 when progress reaches 100%"
  artifacts:
    - path: "app/db/schema.ts"
      provides: "certificates table definition"
    - path: "app/routes/course.$slug.tsx"
      provides: "issuance logic in action"
  key_links:
    - from: "app/routes/course.$slug.tsx"
      to: "certificates table"
      via: "drizzle insert"
---

<objective>
Update the database schema to support certificates and implement the automated issuance logic when a learner completes a course.

Purpose: Ensure achievements are persisted and automatically recognized.
Output: Certificates table in D1 and issuance trigger in course action.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/db/schema.ts
@app/routes/course.$slug.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add certificates table to schema</name>
  <files>app/db/schema.ts</files>
  <action>
    - Add `certificates` table to `app/db/schema.ts` with fields: `id` (primaryKey, UUID), `userId` (references user.id), `courseId` (references courses.id), and `issuedAt` (timestamp).
    - Add `certificatesRelations` for both `user` and `courses`.
    - Update `userRelations` and `coursesRelations` to include the `certificates` many relationship.
    - Run `npm run db:generate` to create a new migration.
    - Run `npm run db:migrate` to apply the migration to local D1.
  </action>
  <verify>
    <automated>npm run db:generate && npm run db:migrate</automated>
  </verify>
  <done>Certificates table exists in local D1 and schema types are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement automated issuance logic</name>
  <files>app/routes/course.$slug.tsx</files>
  <action>
    Modify the `action` function in `app/routes/course.$slug.tsx`:
    - After calculating `progressPercent`, if `progressPercent === 100`:
      - Check if a certificate already exists for this `userId` and `courseId`.
      - If it does not exist, insert a new record into `schema.certificates` with a fresh UUID.
    - Ensure `completedAt` is also set on the `courseProgress` record (already partially implemented, verify it).
  </action>
  <verify>
    <automated>
      # This is hard to test purely via CLI without a test runner.
      # Verification involves marking the last activity as complete and checking D1.
      # We will rely on manual verification in Wave 3, but for now, verify code compiles.
      npm run typecheck
    </automated>
  </verify>
  <done>Action logic includes certificate issuance on 100% completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Action | Completion state is calculated server-side to prevent spoofing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Certificate Issuance | mitigate | Calculate progress and issue certificates exclusively in server-side actions. |
| T-04-02 | Information Disclosure | Certificate ID | mitigate | Use UUIDs (v4) for certificate IDs to prevent enumeration/guessing. |
</threat_model>

<verification>
- Run `npm run db:migrate` and verify success.
- Run `npm run typecheck` to ensure schema changes haven't broken existing code.
</verification>

<success_criteria>
- certificates table exists in D1.
- course action code contains logic to insert into certificates table when progress hits 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/04-certification-completion/04-01-SUMMARY.md`
</output>
