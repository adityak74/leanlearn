---
phase: 04-certification-completion
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [app/routes/course.$slug.tsx, app/routes/dashboard.tsx]
autonomous: false
requirements: [REQ-UI-04]

must_haves:
  truths:
    - "Course sidebar shows a 'View Certificate' button at 100% progress"
    - "Dashboard shows a 'View Certificate' button for completed courses"
  artifacts:
    - path: "app/routes/course.$slug.tsx"
      provides: "Sidebar certificate link"
    - path: "app/routes/dashboard.tsx"
      provides: "Dashboard certificate link"
  key_links:
    - from: "app/routes/course.$slug.tsx"
      to: "/certificate/:id"
      via: "Link component"
    - from: "app/routes/dashboard.tsx"
      to: "/certificate/:id"
      via: "Link component"
---

<objective>
Integrate the certificate links into the user interface so learners can easily access their achievements from the course view and the dashboard.

Purpose: Surface the reward to the user.
Output: Conditional certificate buttons in the UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-certification-completion/04-01-SUMMARY.md
@.planning/phases/04-certification-completion/04-02-SUMMARY.md
@app/routes/course.$slug.tsx
@app/routes/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Certificate Link to Course Sidebar</name>
  <files>app/routes/course.$slug.tsx</files>
  <action>
    - In `app/routes/course.$slug.tsx` loader, also fetch the certificate for the current user and course if it exists.
    - In the `CourseView` component, update the sidebar:
      - If `course.progressPercent === 100` and a certificate exists, render a "🎓 View Certificate" button/link below the progress bar.
      - Style the button to be prominent and use `target="_blank"` to open in a new tab.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Learners can access their certificate directly from the course page.</done>
</task>

<task type="auto">
  <name>Task 2: Add Certificate Link to Dashboard</name>
  <files>app/routes/dashboard.tsx</files>
  <action>
    - In `app/routes/dashboard.tsx` loader, also fetch certificates for the user.
    - Match certificates to courses in the mapped `courses` array.
    - In the `Dashboard` component, for each course card:
      - If the course is completed and has a certificate, show a "View Certificate" link next to or below the "Review Course" button.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Learners can access all earned certificates from their dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end certification flow: issuance at 100%, printable view, and UI links.</what-built>
  <how-to-verify>
    1. Sign in as a learner.
    2. Complete a course (all required activities) or manually set progress to 100% in D1.
    3. Verify that a "View Certificate" button appears in the course sidebar.
    4. Click the button and verify the certificate page loads with correct data.
    5. Press Ctrl+P (or Cmd+P) and verify the print preview is clean and landscape-oriented.
    6. Go to the dashboard and verify the certificate link appears there as well.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → User | Visual confirmation of achievement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | Progress Bar | accept | Progress bar is just a UI element; the actual issuance is protected by server-side logic (Plan 01). |
</threat_model>

<verification>
- Perform manual end-to-end verification as described in the checkpoint.
</verification>

<success_criteria>
- End-to-end flow is functional and visually appealing.
- Certificate is printable and secure.
</success_criteria>

<output>
After completion, create `.planning/phases/04-certification-completion/04-03-SUMMARY.md`
</output>
