---
phase: 03-progress-persistence
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [app/routes/course.$slug.tsx, app/routes/dashboard.tsx]
autonomous: true
requirements: [COURSE-03, UI-02]

must_haves:
  truths:
    - "Dashboard shows a progress bar for each course"
    - "Course page allows marking activities as complete"
    - "Sidebar displays checkmarks for completed activities"
  artifacts:
    - path: "app/routes/course.$slug.tsx"
      provides: "Updated UI with completion controls"
---

<objective>
Update the user interface to display progress information and allow learners to mark activities as completed.

Purpose: Visual feedback and interaction for progress.
Output: Updated Dashboard and Course View with progress bars and completion buttons.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-progress-persistence/03-02-SUMMARY.md
@app/routes/course.$slug.tsx
@app/routes/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dashboard and Course UI</name>
  <files>app/routes/dashboard.tsx, app/routes/course.$slug.tsx</files>
  <action>
    - Update `app/routes/dashboard.tsx`:
      - For each course card, render a progress bar using the `progress_percent` from the backend.
    - Update `app/routes/course.$slug.tsx`:
      - In the sidebar, add a checkmark icon (✅) next to completed activities.
      - In the content area, add a "Mark as Complete" button (or "Completed" state).
      - Clicking the button should call the `POST /api/activity/:id/complete` endpoint.
      - Show a course-level progress bar at the top or in the sidebar.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Learners can track and view their progress visually.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Server | UI interactions trigger server-side updates |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | Progress UI | mitigate | Only show progress for the current authenticated user |
</threat_model>

<success_criteria>
- Dashboard shows progress bars.
- Activity completion is toggleable/markable.
- Visual checkmarks confirm completion.
</success_criteria>

<output>
After completion, create `.planning/phases/03-progress-persistence/03-03-SUMMARY.md`
</output>
