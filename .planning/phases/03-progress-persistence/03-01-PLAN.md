---
phase: 03-progress-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/db/schema.ts, migrations/]
autonomous: true
requirements: [COURSE-03, AUTH-03]

must_haves:
  truths:
    - "D1 database contains activity_completion and course_progress tables"
    - "Drizzle schema reflects the progress tracking model"
  artifacts:
    - path: "app/db/schema.ts"
      provides: "Updated schema with progress tables"
  key_links:
    - from: "app/db/schema.ts"
    - to: "activity_completion"
      via: "Drizzle table definition"
---

<objective>
Update the D1 database schema with the necessary tables for tracking activity completion and overall course progress.

Purpose: Provide the data layer for progress tracking.
Output: Updated schema, new migration, and a local D1 database with the new tables.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-learning-architecture/02-03-SUMMARY.md
@app/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update schema and generate migration</name>
  <files>app/db/schema.ts, migrations/</files>
  <action>
    - Update `app/db/schema.ts` to include:
      - `activity_completion`: user_id (ref user), activity_id (ref activity), completed_at (timestamp).
      - `course_progress`: user_id (ref user), course_id (ref course), progress_percent (integer), completed_at (timestamp), updated_at (timestamp).
    - Add necessary relations to `app/db/schema.ts` for these new tables.
    - Run `npm run db:generate` to create the migration.
    - Apply the migration locally using `npx wrangler d1 migrations apply DB --local`.
  </action>
  <verify>
    <automated>npx wrangler d1 execute DB --local --command ".schema"</automated>
  </verify>
  <done>Tables activity_completion and course_progress exist in the local D1 database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB Migration | Migration script has direct write access to D1 (internal dev tool only) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Migration files | mitigate | Commit migrations to version control for auditability |
</threat_model>

<success_criteria>
- Schema updated with activity_completion and course_progress.
- Local D1 database successfully migrated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-progress-persistence/03-01-SUMMARY.md`
</output>
