---
phase: 02-learning-architecture
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [app/routes/course.$slug.tsx, app/routes.ts]
autonomous: true
requirements: [COURSE-01, UI-03]

must_haves:
  truths:
    - "Navigating to /course/:slug loads the correct course structure"
    - "Course sidebar displays chapters and activities in sort_order"
    - "Course page shows initial activity content"
  artifacts:
    - path: "app/routes/course.$slug.tsx"
      provides: "Course Sidebar and Content Viewer"
    - path: "app/routes.ts"
      provides: "New route registration"
---

<objective>
Create the course structure viewer with a sidebar for navigation and a main content area for activities.

Purpose: Provides the primary learning interface.
Output: New React route component and updated routing table.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-learning-architecture/02-RESEARCH.md
@app/routes.ts
@app/routes/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register and Create Course Route</name>
  <files>app/routes.ts, app/routes/course.$slug.tsx</files>
  <action>
    - Register `course/:slug` in `app/routes.ts`.
    - Create `app/routes/course.$slug.tsx`.
    - Implement the loader:
      - Fetch course data (including chapters and activities) using the slug from the URL params.
      - Fetch from Drizzle using `context` or call the `/api/course/:slug` Hono endpoint.
    - Implement the component UI:
      - Sidebar showing the course title, then mapping through chapters and their activities.
      - Main content area to display the selected (or first) activity.
      - Use standard CSS or Tailwind if applicable (project context seems minimal so basic CSS is fine).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Course page successfully renders the "Hypermemory" course structure.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Course Navigation</name>
  <files>app/routes/course.$slug.tsx</files>
  <action>
    - Enhance the sidebar in `course.$slug.tsx` to handle activity selection.
    - For Phase 2, this can just use URL fragments or search params (e.g., `?activityId=...`) or simple state if it stays single-page for activities.
    - Recommendation: Use a child route or a search param for current activity. For now, a search param `?a=activity_id` is leanest.
    - Update activity links in the sidebar to use the search param.
    - Render the corresponding activity's content (title + text/video) in the main area.
  </action>
  <verify>
    <automated>Visit /course/hypermemory?a=... (manual test)</automated>
  </verify>
  <done>Clicking an activity in the sidebar updates the main content area with its details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Server Loader | Loader has direct access to context and DB |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Elevation of Privilege | course.$slug.tsx | mitigate | Check session in loader and ensure user is authenticated |
</threat_model>

<success_criteria>
- Navigation to `/course/hypermemory` works.
- Sidebar reflects the seeded chapters and activities.
- Activity content (title and body) updates on selection.
</success_criteria>

<output>
After completion, create `.planning/phases/02-learning-architecture/02-03-SUMMARY.md`
</output>
