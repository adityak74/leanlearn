---
phase: 02-learning-architecture
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [app/server.ts, app/routes/dashboard.tsx]
autonomous: true
requirements: [COURSE-01, UI-02, AUTH-03]

must_haves:
  truths:
    - "GET /api/me/courses returns a list of available courses"
    - "GET /api/course/:slug returns the course hierarchy (chapters and activities)"
    - "Dashboard displays the Hypermemory course"
  artifacts:
    - path: "app/server.ts"
      provides: "Course API endpoints"
  key_links:
    - from: "app/routes/dashboard.tsx"
    - to: "/api/me/courses"
      via: "Direct DB access in loader or fetch"
---

<objective>
Implement Hono API routes for fetching course data and update the Learner Dashboard to list available courses.

Purpose: Bridges the database and the UI with real course data.
Output: Working Hono endpoints and a data-driven dashboard list.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-learning-architecture/02-RESEARCH.md
@app/server.ts
@app/routes/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Course API in Hono</name>
  <files>app/server.ts</files>
  <action>
    - Update `app/server.ts` to add course fetch logic using Drizzle and the Hono context (`c.env.DB`).
    - Endpoint: `GET /api/me/courses`
      - Fetch all courses from `courses` table.
      - Later this will be filtered by enrollment, but for MVP it returns all.
    - Endpoint: `GET /api/course/:slug`
      - Fetch course by slug.
      - Nested fetching: Fetch chapters for the course, and activities for each chapter, using `sort_order` for deterministic display.
    - Ensure routes are protected by the existing session middleware.
  </action>
  <verify>
    <automated>curl -i http://localhost:8787/api/me/courses</automated>
  </verify>
  <done>Hono endpoints return correct JSON structure for courses, chapters, and activities.</done>
</task>

<task type="auto">
  <name>Task 2: Update Dashboard to list courses</name>
  <files>app/routes/dashboard.tsx</files>
  <action>
    - Update the loader in `app/routes/dashboard.tsx` to fetch courses from D1 (either via direct Drizzle access in loader or by calling the new `/api/me/courses` endpoint).
    - Update the UI to map through the fetched courses.
    - Render each course with its title, description, and a "Start Course" link pointing to `/course/${course.slug}`.
  </action>
  <verify>
    <automated>Visit /dashboard (manual via browser) after logging in.</automated>
  </verify>
  <done>The Dashboard displays the "Hypermemory" course dynamically from the database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Hono API | API endpoints exposed to the frontend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | GET /api/course/:slug | mitigate | Ensure session middleware is applied before course data access |
| T-02-02-02 | Insecure Direct Object Reference | GET /api/course/:slug | mitigate | Validate slug and ensure course exists; in future, check enrollment |
</threat_model>

<success_criteria>
- Hono API successfully serves course data from D1.
- Dashboard lists courses fetched from the backend.
- Course navigation links correctly formatted.
</success_criteria>

<output>
After completion, create `.planning/phases/02-learning-architecture/02-02-SUMMARY.md`
</output>
