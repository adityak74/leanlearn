---
phase: 02-learning-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/db/schema.ts, package.json]
autonomous: true
requirements: [COURSE-01, COURSE-02, COURSE-03, STACK-02]

must_haves:
  truths:
    - "D1 database contains courses, chapters, and activities tables"
    - "Initial Hypermemory course data is present in the database"
  artifacts:
    - path: "app/db/schema.ts"
      provides: "Course schema definition"
    - path: "app/db/seed.ts"
      provides: "Database seeding logic"
  key_links:
    - from: "app/db/seed.ts"
    - to: "app/db/schema.ts"
      via: "Drizzle imports"
---

<objective>
Define the database schema for the course hierarchy (Course > Chapter > Activity) and seed the initial "Hypermemory" course data.

Purpose: Establishes the data foundation for the learning platform.
Output: Updated schema, new migration, and a populated local D1 database.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-learning-architecture/02-RESEARCH.md
@app/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update schema and generate migration</name>
  <files>app/db/schema.ts, migrations/</files>
  <action>
    - Update `app/db/schema.ts` to include `courses`, `chapters`, and `activities` tables per `02-RESEARCH.md`.
    - Ensure `sort_order` (integer) is present on `chapters` and `activities`.
    - Ensure `required` (integer, mode: boolean, default 1) is present on `activities`.
    - Use `text("id").primaryKey()` for all IDs (compatible with `cuid2` or `uuid` strings).
    - Run `npm run db:generate` to create the migration.
    - Apply the migration locally using `npx wrangler d1 migrations apply leanlearn-db --local`.
  </action>
  <verify>
    <automated>npx wrangler d1 execute leanlearn-db --local --command ".schema"</automated>
  </verify>
  <done>Tables courses, chapters, and activities exist in the local D1 database.</done>
</task>

<task type="auto">
  <name>Task 2: Create and run seed script</name>
  <files>app/db/seed.ts, package.json</files>
  <action>
    - Create `app/db/seed.ts` to populate the database with the "Hypermemory" course structure.
    - Course: title="Hypermemory", slug="hypermemory".
    - Chapters: "Foundations of Memory" (sort_order: 1), "The Method of Loci" (sort_order: 2).
    - Activities: 3+ activities spread across chapters, mixing "text" and "video" types.
    - Update `package.json` to add a `db:seed` script: `tsx app/db/seed.ts`.
    - Run the seed script against the local D1 instance (using WRANGLER_D1_DB_ID or similar environment variables if needed for `tsx` to connect, or use `wrangler d1 execute` with a SQL file if direct TS execution is tricky with local D1).
    - RECOMMENDED: Since `tsx` might not easily access local D1 miniflare state without extra config, create the seed as a SQL script OR use `wrangler d1 execute` with a generated file. Alternatively, use `drizzle-orm` in the seed script and point it to the local sqlite file in `.wrangler/state/v3/d1/...`.
  </action>
  <verify>
    <automated>npx wrangler d1 execute leanlearn-db --local --command "SELECT count(*) FROM courses;"</automated>
  </verify>
  <done>Database contains 1 course, 2 chapters, and at least 3 activities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB Seed | Seed script has direct write access to D1 (internal dev tool only) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Migration files | mitigate | Commit migrations to version control for auditability |
</threat_model>

<success_criteria>
- Schema updated with courses, chapters, and activities.
- Local D1 database successfully migrated.
- Seed data for "Hypermemory" course verified in database.
</success_criteria>

<output>
After completion, create `.planning/phases/02-learning-architecture/02-01-SUMMARY.md`
</output>
