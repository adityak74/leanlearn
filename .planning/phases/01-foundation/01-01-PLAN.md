---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.ts, react-router.config.ts, app/server.ts, wrangler.jsonc, .dev.vars]
autonomous: true
requirements: [STACK-01, STACK-03]

must_haves:
  truths:
    - "Project scaffolds successfully with React Router v7 and Hono"
    - "Local development server starts with Hono and Vite"
    - "Wrangler is configured with necessary bindings and secrets"
  artifacts:
    - path: "app/server.ts"
      provides: "Hono server entry point"
    - path: "wrangler.jsonc"
      provides: "Cloudflare configuration"
  key_links:
    - from: "vite.config.ts"
      to: "app/server.ts"
      via: "Hono/Vite adapter"
---

<objective>
Scaffold the foundational project structure using React Router v7 and Hono, optimized for Cloudflare Pages deployment.

Purpose: Establish the unified server/client architecture required for lean deployment.
Output: Working project skeleton with environment configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold React Router v7 + Hono Project</name>
  <files>package.json, vite.config.ts, react-router.config.ts, app/server.ts, app/root.tsx, app/routes/_index.tsx</files>
  <action>
    Initialize a new React Router v7 project using Vite. Integrate Hono as the server entry point using `hono-react-router-adapter`.
    - Install core dependencies: `hono`, `react-router`, `hono-react-router-adapter`.
    - Install dev dependencies: `@react-router/dev`, `@react-router/cloudflare`, `@hono/vite-dev-server`, `wrangler`.
    - Create `app/server.ts` as the Hono entry point that wraps the React Router handler.
    - Configure `vite.config.ts` to use the Hono and React Router plugins.
    - Configure `react-router.config.ts` for Cloudflare preset.
  </action>
  <verify>
    <automated>npm run dev -- --port 5173 --host 0.0.0.0 & sleep 5 && curl http://localhost:5173 && kill $!</automated>
  </verify>
  <done>Development server starts and renders the default React Router index page via Hono.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Cloudflare Environment</name>
  <files>wrangler.jsonc, .dev.vars</files>
  <action>
    Set up the Cloudflare environment configuration.
    - Create `wrangler.jsonc` (or `wrangler.toml`) with:
      - Compatibility date.
      - D1 database binding named `DB`.
      - Assets configuration for Cloudflare Pages.
    - Create `.dev.vars` for local development with placeholders for:
      - `BETTER_AUTH_SECRET`
      - `BETTER_AUTH_URL=http://localhost:5173`
      - `GOOGLE_CLIENT_ID`
      - `GOOGLE_CLIENT_SECRET`
  </action>
  <verify>
    <automated>grep -q "DB" wrangler.jsonc && grep -q "BETTER_AUTH_SECRET" .dev.vars</automated>
  </verify>
  <done>Cloudflare configuration is ready for D1 integration and local development.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Hono | External untrusted requests enter the system here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | .dev.vars | mitigate | Add .dev.vars to .gitignore immediately |
</threat_model>

<verification>
Check if `npm run dev` serves the application through the Hono entry point.
</verification>

<success_criteria>
- Project structure follows the research recommendation.
- Hono serves the React Router application.
- Wrangler configuration includes D1 binding.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
