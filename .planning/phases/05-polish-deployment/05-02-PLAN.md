---
phase: 05-polish-deployment
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [package.json, vitest.config.ts, app/server.ts, tests/profile.test.ts, tests/certification.test.ts]
autonomous: true
requirements: [REQ-CERT-01, REQ-STACK-01]
must_haves:
  truths:
    - "Application has an automated test suite"
    - "Profile loader correctly handles data aggregation"
    - "Certificate issuance logic is verified via tests"
    - "Static assets have appropriate Cache-Control headers"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Testing configuration"
    - path: "tests/profile.test.ts"
      provides: "Integration test for profile logic"
    - path: "tests/certification.test.ts"
      provides: "Test for automatic certificate issuance"
---

<objective>
Setup testing infrastructure to ensure application quality and optimize performance via Hono middleware for production-grade asset serving.

Purpose: Prevent regressions and ensure the "Lean" experience remains fast.
Output: Vitest configuration, integration tests, and performance-optimized server.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-deployment/05-RESEARCH.md
@app/server.ts
@app/routes/profile.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Testing Infrastructure and Integration Tests</name>
  <files>package.json, vitest.config.ts, tests/profile.test.ts, tests/certification.test.ts</files>
  <action>
    - Install `vitest` and `@testing-library/react` as dev dependencies.
    - Create `vitest.config.ts` with basic configuration for React/Node.
    - Update `package.json` with `"test": "vitest run"`.
    - Create `tests/profile.test.ts` to verify the logic of the Profile loader (mocking DB).
    - Create `tests/certification.test.ts` to verify that reaching 100% progress correctly triggers certificate issuance (as per REQ-CERT-01).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Testing framework setup and integration tests for core logic passing.</done>
</task>

<task type="auto">
  <name>Task 2: Performance Optimization (Caching Headers)</name>
  <files>app/server.ts</files>
  <action>
    - Update Hono `app/server.ts` to add a middleware that sets `Cache-Control` headers for static assets.
    - Use `public, max-age=31536000, immutable` for assets in `/assets/` directory.
    - Ensure headers are only applied to static files, not dynamic routes.
    - Verify "Lean" bundle size by running a production build.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Caching headers implemented in the Hono server to improve performance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing Env → Code | Test execution boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Denial of Service | Static Assets | mitigate | Implement long-term caching to reduce server load and egress |
</threat_model>

<verification>
Run `npm test` to verify logic. Run `npm run build` and inspect the output to ensure assets are generated correctly.
</verification>

<success_criteria>
- `npm test` command exists and passes tests.
- Hono server includes cache headers for static assets.
- Certification issuance logic is validated by a test case.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-deployment/05-02-SUMMARY.md`
</output>
