---
phase: 05-polish-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/styles/app.css, app/root.tsx, app/routes/profile.tsx, app/routes.ts, app/routes/dashboard.tsx]
autonomous: true
requirements: [REQ-UI-04, REQ-UI-02]
must_haves:
  truths:
    - "Application uses a global CSS system for styling"
    - "User can view their profile with course statistics"
    - "Navigation exists between Dashboard and Profile"
  artifacts:
    - path: "app/styles/app.css"
      provides: "Global design tokens and base styles"
    - path: "app/routes/profile.tsx"
      provides: "Learner profile view with stats"
  key_links:
    - from: "app/routes/dashboard.tsx"
      to: "/profile"
      via: "Link component"
---

<objective>
Establish a global "Lean" styling system and implement the Learner Profile page to display aggregated statistics and certificates.

Purpose: Provide users with a centralized view of their achievements and unify the application's visual language.
Output: Global CSS file, Profile route, and polished navigation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-deployment/05-RESEARCH.md
@app/root.tsx
@app/routes/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Global CSS and Design Tokens</name>
  <files>app/styles/app.css, app/root.tsx</files>
  <action>
    - Create `app/styles/app.css` with CSS variables for:
      - `--primary`: #0070f3
      - `--success`: #10b981
      - `--neutral`: #f3f4f6
      - `--text-main`: #111827
      - `--text-muted`: #666
      - `--bg-page`: #ffffff
      - `--border`: #eeeeee
    - Add base resets and typography (Inter).
    - Update `app/root.tsx` to include `app/styles/app.css` via the `links` export.
  </action>
  <verify>
    <automated>grep "app/styles/app.css" app/root.tsx</automated>
  </verify>
  <done>Global CSS file created and linked in the root layout.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Profile Route and Loader</name>
  <files>app/routes/profile.tsx, app/routes.ts</files>
  <action>
    - Register `profile` route in `app/routes.ts`.
    - Implement `app/routes/profile.tsx` with a loader that:
      - Authenticates user (redirect to /login if missing).
      - Fetches all `courseProgress` for the user.
      - Fetches all `certificates` for the user.
      - Uses `Promise.all` to fetch data in parallel as per 05-RESEARCH.md.
      - Calculates stats: Total Courses, Average Progress %, Certificates Earned.
    - Build UI displaying:
      - User name/email.
      - Stats cards.
      - List of earned certificates with links to `/certificate/:id`.
      - Back to Dashboard link.
  </action>
  <verify>
    <automated>grep "route(\"profile\", \"routes/profile.tsx\")" app/routes.ts</automated>
  </verify>
  <done>Profile page implemented and accessible at /profile.</done>
</task>

<task type="auto">
  <name>Task 3: Unify Navigation and Dashboard Styling</name>
  <files>app/routes/dashboard.tsx</files>
  <action>
    - Add a "View Profile" link to the Dashboard header.
    - Refactor Dashboard to use global CSS classes from `app/styles/app.css` instead of complex inline styles where possible.
    - Ensure a consistent "Lean" layout across Dashboard and Profile.
  </action>
  <verify>
    <automated>grep "/profile" app/routes/dashboard.tsx</automated>
  </verify>
  <done>Dashboard linked to Profile and styles unified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Profile data fetched via loader |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Profile Loader | mitigate | Ensure loader strictly filters by `user.id` for all DB queries |
</threat_model>

<verification>
Manual verification: Sign in as a user with some progress and navigate between Dashboard and Profile. Check that stats are accurate.
</verification>

<success_criteria>
- Global CSS is active and linked in root.
- Profile page displays correct statistics (Total courses, average progress, certs count).
- User can navigate Dashboard <-> Profile.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-deployment/05-01-SUMMARY.md`
</output>
