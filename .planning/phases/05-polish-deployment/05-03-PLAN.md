---
phase: 05-polish-deployment
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [wrangler.jsonc, package.json]
autonomous: false
requirements: [REQ-STACK-01]
user_setup:
  - service: Cloudflare
    why: "Production deployment"
    env_vars:
      - name: BETTER_AUTH_URL
        source: "Set in Cloudflare Pages dashboard settings"
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console"
must_haves:
  truths:
    - "Production build is successful"
    - "Wrangler configuration is valid"
  artifacts:
    - path: "wrangler.jsonc"
      provides: "Cloudflare deployment config"
---

<objective>
Finalize production configuration and perform a deployment smoke test to ensure the application is ready for users on Cloudflare.

Purpose: Transition from local development to a stable production environment.
Output: Validated wrangler config and successful production build.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-deployment/05-RESEARCH.md
@wrangler.jsonc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production Infrastructure Configuration</name>
  <files>wrangler.jsonc, package.json</files>
  <action>
    - Update `wrangler.jsonc` with production-ready settings:
      - Ensure `pages_build_output_dir` is `./build/client`.
      - Add a comment/placeholder reminding the user to set the actual D1 `database_id`.
      - Verify `compatibility_flags` includes `nodejs_compat`.
    - Add a `deploy` script to `package.json`: `"deploy": "npm run build && wrangler pages deploy ./build/client"`.
  </action>
  <verify>
    <automated>npx wrangler deploy --dry-run</automated>
  </verify>
  <done>Deployment scripts and configuration finalized for Cloudflare.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Deployment Readiness</name>
  <action>Human verification of the production build and deployment configuration.</action>
  <what-built>Deployment Configuration and Production Build</what-built>
  <how-to-verify>
    1. Run `npm run build` to ensure the production build still passes.
    2. Verify `wrangler.jsonc` contains the correct project name and D1 binding name.
    3. Ensure `BETTER_AUTH_URL` is configured in the Cloudflare Dashboard to match your production domain.
    4. (Optional) Run `npm run deploy` if you have active Cloudflare credentials and a D1 database ready.
  </how-to-verify>
  <verify>Manual confirmation from user.</verify>
  <done>User approved the deployment configuration.</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI/CD → Cloudflare | Secret injection boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | Deployment Config | mitigate | Ensure sensitive keys (GOOGLE_CLIENT_SECRET) are NOT committed in wrangler.jsonc; use environment variables instead. |
</threat_model>

<verification>
Verify build success and `wrangler deploy --dry-run` output.
</verification>

<success_criteria>
- Production build succeeds.
- `package.json` contains a functional `deploy` script.
- Wrangler configuration is valid for Cloudflare Pages/Workers.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-deployment/05-03-SUMMARY.md`
</output>
